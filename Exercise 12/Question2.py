#!/usr/bin/env python3

#* this is setting the variable as tup "type" str
tup = 'Hello'
print(len(tup))

#* a tup with a single item must include a trailing comma
tup = 'Hello',
print(len(tup))

#* check the tuype of the variable
print(type(tup))